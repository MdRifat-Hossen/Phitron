#include <bits/stdc++.h>
using namespace std;
int main()
{

    //   1.myList.size()->Returns the size of the list
    // list<int> mylist = {10, 20, 30};
    // cout << mylist.max_size() << endl;

    // 2.myList.max_size()->Returns the maximum size that the vector can hold.
    list<int> mylist = {10, 20, 30};
    // cout << mylist.max_size() << endl;
    // for (int val : mylist)
    // {
    //     cout << val << endl;
    // }

    // 3.myList.clear()->Clears the list elements Do not delete the memory only clear the list
    // mylist.clear();
    // for (int val : mylist)
    // {
    //     cout << val << endl;
    // }
    // cout << mylist.size() << endl;

    // 4.myList.empty()->Return true/false if the list s empty or not.

    // 5.myList.resize()->Change the size of the lis

    return 0;
}